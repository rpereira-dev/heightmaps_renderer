#include "renderer.h"

void cameraInit(t_camera * camera) {
	(void)camera;
}

void cameraDelete(t_camera * camera) {
	(void)camera;
}

void cameraUpdate(t_camera * camera) {
	(void)camera;
}